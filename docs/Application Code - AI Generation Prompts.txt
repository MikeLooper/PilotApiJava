This file for for *human readers* ONLY - AI processes should NOT read or consume in any way.


The code in this application was generated by AI using the following prompts:
==============================================================================

After the first 2 prompts, the majority of the prompts used, were suggestions by the AI.

1.
"Create a directory structure that is appropriate for a Java Spring Boot API, following best practices.
Do not generate any code at this time, that will occur later.
Include a 'docs' directory.
Create a report of the actions taken here, in the 'docs' directory."

2.
For the text of the application generation prompt, see: .\spring-boot-openapi-api-generation-prompt.md

"Execute the prompt found in the currently open file.
Create a document in the ".\docs" directory that describes the actions taken by this prompt."

3.
Created document: C:\Working\Storage\Dev\GitHub\PilotApiJava\docs\prompt-execution-actions-report.md

"Implement the defined code generation."

4.
"Add focused unit and controller tests for all operations."

5.
"Break apart the ApiControllersWebMvcTest.java file, so each domain is in a separate file."

6.
"Apply the same split-by-domain pattern to service tests next (one file per resource service)."

7.
"Update unit tests to follow the pattern: Arrange, Act, Assert.
Update unit test method names to follow the format: '<Name_Of_Class_Under_Test>_<Name_Of_Method_Or_Property_To_Test>_<Expected_Result>_Test'.
Apply the same naming convention to the controller WebMvc tests."

8.
"Install Maven and run test execution to verify runtime behavior."

"Uninstall the local install of Maven.  Then provide me with the chocolalty command to install Maven on my system."

"Maven has been installed on my system.  Verify it is working for this application."

9. 
"The SQL Server database that should be used by this API has the following attributes:
	- Database Name = NorthWind
	- ConnectTimeout = 30
	- Host = local_mssql
	- Password = <DevUser password>
	- Port = 1433
	- UserName = DevUser
	- Schema = dbo
Update this application to use these database attributes to connect to the database."

"Run the app and perform a live DB connectivity check against local_mssql to confirm runtime connection/authentication."

"Update Flyway SQL Server support in pom dependencies (I can patch this next and re-verify full startup)."

"Make updates in this application to match Northwind database tables.
Make code or configuration changes necessary to enable use of Northwind database with this API code.
Do not make any changes to the database."

"add a dedicated profile (for example northwind-localhost) so local verification can be run without typing environment overrides each time.

"add a Maven wrapper-friendly alias in README for this exact command so it is copy-paste discoverable for my team."

10.
"Update the application port from 8080 to 56661."
"Add the ability for the application port to be overridden from an environment variable."
	> The port now reads from the SERVER_PORT environment variable, falling back to 56661 if it's not set.
"Update the README to include instructions on how to override the application port."

"Add a second database connection for PostgreSQL, while keeping the ability to connect to SQL Server.  Include the ability to switch back and forth based upon a configuration setting.  Update the README with instructions on how to switch between the two databases."

11.
Include the `https://github.com/MikeLooper/PilotSharedSource` repository in this workspace as a SubModule.  Ensure the latest copy of the repository is retrieved and ready for use.
Update the README to describe how the SubModule can be retrieved and updated when there is a change in the SubModule.  Place the SharedModule under a `shared` directory.

12.
Update every endoint to include the following:
- Request headers:
	Accept:		"application/json"
	ApiVersion:	<default to the current API version>
- Response headers:
	Content-Type:	"application/json"
	Date:			<the curent date and time>
	ApiVersion:		<the current API version>

Update every endpoint to return a JSON object.

<Correct changes that AI made without being told to:>
Update every controller as follows:
- A Delete endpoint should return a 204 for a successfull call and return the value from the service.
- An Update endpoint should return a 204 for a successfull call and return the value from the service.
The Update and Delete endpoints should not return Void.  They should return the number of records that were changed by the database commands.

For the Delete and Update endpoints, change the service logic so that:
- If the count of updated rows is positive, a true is returned to the controller; aelse a false is returned.
- If the value returned to the controller is true, a status 204 is returned.
- If the value returned to the controller is false, a status 400 is returned.

13.
Update the GetAll endpoints to add pagination, with two optional query parameters:
- `page` = the target page index.  1-based, meaning the first page is 1.
	- Default: 0
- `pageSize` = how many items return per page.
	- Default: 20
If `page` = 0, do not use paging - return all rows.
This pagination will control how data is retieved from the database.

