server:
  port: ${SERVER_PORT:59999}

app:
  table:
    order-details: "[Order Details]"
  security:
    provider-url: ${APP_SECURITY_PROVIDER_URL:http://localhost:55001}

otel:
  exporter:
    otlp:
      endpoint: ${OTEL_EXPORTER_OTLP_ENDPOINT:http://localhost:4317}

spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=NorthWind;encrypt=true;trustServerCertificate=true;connectTimeout=30
    username: DevUser
    password: Hjm$435yVt7a
  jpa:
    properties:
      hibernate:
        default_schema: dbo
  flyway:
    default-schema: dbo
