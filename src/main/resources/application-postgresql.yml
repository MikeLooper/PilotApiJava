spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/northwind}
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        default_schema: ${DB_SCHEMA:public}
  flyway:
    default-schema: ${DB_SCHEMA:public}
